#include <iostream>

int main()
{
    // The const keyword specifies that a variable's value is constants
    // tells the compiler to prevent anything from modifying it
    // (read-only).

    const double PI = 3.14159;
    const double LIGHT_SPEED = 2999499393;
    const int WIDTH = 1920;
    const int HEIGHT = 1080;
    return 0;
}